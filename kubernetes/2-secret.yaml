apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: grocery-app
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "989878"
  MYSQL_DATABASE: "GROCERY"
  # Store these securely - use base64 encoding for production
  # To create: echo -n 'password' | base64
  # This is demonstrating the concept, not secure for production